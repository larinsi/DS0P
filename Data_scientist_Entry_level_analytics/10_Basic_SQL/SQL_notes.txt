use (имя базы) // Подключение к базе данных
SELECT (имя столбцов через запятую или * для выбора всего; DISTINCT для выбора уникальных значений) FROM (имя таблицы) // Запрос
WHERE (условие выбора) // Фильтр запроса
ORDER BY (имя столбца) // Сортировка ASC-по возрастанию, DESC-по убыванию
LIMIT (число) // Число первых записей запроса для вывода

UNION // Объединяет запросы (Должно быть одинаковое количество столбцов в каждом запросе), по умолчанию выкидывает повторения. Для включения повторений модификатор ALL

AS // Присваивание имени 

DATEDIFF(x, y) // Разница дат между x и y, в днях
NOW() // Текущая дата

IF(условие, "Значение при выполнении условия", "Значение при не выполнении условия") // Функция условия

CONCAT() //Конкатенация строк

COUNT(x) // Число записей в x
AVG(x) // Среднее значение по столбцу x
MAX(x) // Максимальное значение из столбца x
MIN(x) // Минимальное значение из столбца x
SUM(x) // Сумма всех значений из столбца х

JOIN (имя таблицы) ON (условие соеднения) // Присоединение таблицы по условию

GROUP BY (х) // Группирует по значениям из столбца х (Всегда перед ORDER BY)

